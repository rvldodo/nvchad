local options = {
  ensure_installed = { "lua", "javascript", "typescript", "tsx", "go", "css" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
  --
  -- autotag = {
  --   enable = true
  -- }
}

return options
